#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a[20],b[20],alice=0,bob=0;
    scanf("%d %d %d",&a[0],&a[1],&a[2]);
    scanf("%d %d %d",&b[0],&b[1],&b[2]);
    if(a[0]>b[0]&&)
        alice++;
    else
        bob++;
    if(a[1]>b[1])
        alice++;
    else
        bob++;
    if(a[2]>b[2])
        alice++;
    else
        bob++;
   printf("%d %d",alice,bob);
    return 0;
}
